const $=s=>document.querySelector(s);const api=async(path,opt={})=>{const r=await fetch(path,{credentials:'same-origin',headers:{'Content-Type':'application/json'},...opt});const data=await r.json();if(!r.ok)throw Error(data.error);return data};let me=null,last='';const esc=s=>String(s).replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));
function show(user){me=user;$('#auth').hidden=true;$('#chat').hidden=false;$('#profile').innerHTML=`<span>${esc(user.name)} · ${user.role==='ccs'?'CCS':'Technicien'}</span><button id="logout">Quitter</button>`;$('#logout').onclick=async()=>{await api('/api/logout',{method:'POST'});location.reload()};loadMessages()}
async function loadMessages(){if(!me)return;const items=await api('/api/messages');const html=items.map(m=>`<article class="message ${m.user_id===me.id?'mine':''}"><div><b>${esc(m.name)}</b><span>${m.role==='ccs'?'CCS':'Technicien'} · ${new Date(m.created_at*1000).toLocaleTimeString('fr-FR',{hour:'2-digit',minute:'2-digit'})}</span></div><p>${esc(m.body)}</p></article>`).join('');if(html!==last){last=html;$('#messages').innerHTML=html;$('#messages').scrollTop=$('#messages').scrollHeight}}
$('#auth-form').onsubmit=async e=>{e.preventDefault();try{show(await api('/api/enter',{method:'POST',body:JSON.stringify({name:$('#name').value,role:$('#role').value})}))}catch(err){$('#error').textContent=err.message}};$('#message-form').onsubmit=async e=>{e.preventDefault();try{await api('/api/messages',{method:'POST',body:JSON.stringify({body:$('#message').value})});$('#message').value='';await loadMessages()}catch(err){alert(err.message)}};api('/api/me').then(user=>user&&show(user));setInterval(()=>me&&loadMessages().catch(()=>{}),2500);
